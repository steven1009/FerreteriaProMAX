@model IEnumerable<FerreteriaProMAX.Models.DetalleProvProd>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Fecha)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.proveedores.Nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Fecha)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.proveedores.Nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdDetalleProvProd }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdDetalleProvProd }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdDetalleProvProd })
        </td>
    </tr>
}

</table>
